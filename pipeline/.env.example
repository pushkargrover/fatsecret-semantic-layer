# Copy to pipeline/.env and fill in. Never commit real secrets (.env is gitignored).

# FatSecret Platform API — OAuth 2.0 client credentials.
# Get these at https://platform.fatsecret.com (Manage -> Apps). Basic tier is free
# but requires IP allowlisting. Read the T&Cs on caching/redistribution first.
FATSECRET_CLIENT_ID=
FATSECRET_CLIENT_SECRET=
